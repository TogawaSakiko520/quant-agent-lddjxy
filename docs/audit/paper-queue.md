# Alpaca Paper休市提交撤单验收（2026-09-20）

> 历史记录：以下结论、路径及授权仅适用于文中日期和基线；后续实现与验证见[当前状态](../../PROJECT_STATE.md)。本地 `artifacts/` 证据未随 Git 分发，部分旧产物可能已不可用。

用户在MA真实计划之后，明确要求现在测试提交订单、取得远端ID、查询及撤单，并按该范围验收后提交/推送GitHub。本轮验收是订单操作链路，实际成交、成交入账、卖出及正式策略收益验证仍单列未验证；不把取消成功等同买卖成交。原MA及双因子设计和风险数值保留。

## 基线、实现与来源

`artifacts/paper-queue-20260920/baseline`保存含前轮成果的148文件基线，HEAD为f48cd4fc8dfc15d55c45655c08a4a957771694c2。凭据仍只从用户指定的私有.env读取，不进入Git或报告；没有更换账户或使用全账户购买力。

[官方个人Trading API订单说明](https://docs.alpaca.markets/us/docs/orders-at-alpaca)确认非扩展时段DAY单在收盘后可排队到下一交易日，accepted不代表成交；[官方Python SDK订单接口](https://alpaca.markets/sdks/python/api_reference/trading/orders.html)区分提交、查询和单笔撤销。继续使用已锁定alpaca-py0.44.0及固定Paper主机，没有使用其他业务的Broker API或新增依赖。

显式 `paper-execute --queue-cancel-test`只取MA首个可行目标的一股，真实最后完整原始收盘价限价、不高于该价；最大一笔且含费用不超过500美元。远端和保存日历均须休市，下一开盘一致且至少15分钟以后；空仓、无其他订单、资金与集中度等仍经共同风险计算。普通策略的开市/新鲜报价检查不改，不能把旧报价改成现在。该模式使用同一ExecutionService持久化、恢复与唯一Broker调用，取得远端事实后立即仅撤本单，最多60秒核对；明确未知时不重发。

## 独立复核与失败记录

非作者复核记录为上述证据目录review-risk.md及review-application.md。独立反例关闭三个问题：对账差异不应阻止撤销已知本轮单、普通订单目录不能被测试模式接管、测试意外成交不能冒充正式策略验收。OrderIntent字段与默认哈希保留，新增PaperQueueTestContext及派生Schema/映射已同步。journal同时记录订单与账务，测试精确验证intent/OPEN/CANCELED审计及无成交events，不能误要求删掉订单审计。

首次真实plan-01尝试在任何POST前被UTC契约校验拒绝：官方clock返回纽约偏移。内部和远端订单均为0，失败日志与观察保留；修复为检查明确时区后把同一时刻规范为UTC，无时区数据拒绝。plan-02未执行；后续plan-03保留新身份，不改旧封印。

离线开发初始样本日历超出请求范围，按官方返回范围修正样本；应用测试又发现适配器原开盘资格限制，新增与同一上下文绑定的时钟/身份/单股/金额二次校验。全检查01为两环境408项通过；检查02为补充无时区回归而主动中断，不能计通过；检查03同时运行两环境共享测试账户触发正确的非阻塞账户锁拒绝，未修改锁或弱化断言，后续改为顺序运行。最终结果在完成后追加。

## 真实执行及边界

指定范围仍是原10只美股、独立策略预算10000 USD。重新只读确认账户空仓无订单，现金100000 USD；MA使用8月21日至9月18日的20日窗口，目标仍为AAPL1、GOOGL1、XOM2。本次仅AAPL1股，336.13美元限价、1美元费用预留，共337.13美元风险预留；费用预留不是实际手续费。

第二次尝试计划身份 `1e363d5e606b9a008684e8fce4b83ce1dd735eaa9856f4e37d29a0a04887b14d`，源码指纹 `07be0b052412b809fb4acf317202d0cb0a41e0861744f629c77a30c205eef072`。原输入、逐次观察和用户授权记录保存于本轮证据目录，实际远端订单、撤单、核对和重启结果在取得后追加。

## 预提交时间偏差回归

plan-03在适配器POST前被拒绝；本地意图随后保守变为UNKNOWN，远端订单ID为空。再次按原客户ID查询404且全量订单为空，未据此直接换身份重发。独立三次只读时钟观察显示服务器领先本机约0.12秒，触发原严格未来时钟拒绝。补充最多1秒的实际等待后仍严格检查0—60秒时间、至少15分钟开盘间隔；超过1秒或等待后未追上仍拒绝，不放宽风险或重写事实。

旧源码完整重建于legacy-source，按原有src/tools/configs路径及内容哈希算法精确复现07be0b052412b809fb4acf317202d0cb0a41e0861744f629c77a30c205eef072。明确错误消息在唯一POST之前；新版本将请求准备与POST分开，仅准备失败使用OrderNotSent追加本地拒绝，发送后不确定性保留。旧意图通过显式、固定版本的证据契约维护，要求原失败、原代码、唯一原意图、无成交且无其他差异；404本身不足以证明未发送。维护追加审计，不修改现金或删除UNKNOWN历史。实际结果后续追加。

## 真实受理、撤销与分页修复

plan-04的计划ID为c4c6f8f7a821443d91d9a1e6029e2bfff84658ec6222ac7f48047e96a73ed9b6，提交时源码2d39ac3cd4033e1732da79d15fc35272360b723782398dd8516b031bf8c19faf。旧意图经证据维护追加REJECTED、核对一致后，才启动这次明确授权测试。Alpaca返回订单ID **981caa56-2789-4944-9c4b-4f0c17149e2c**、客户ID queue-069cd099204b5c9748e003187a76a939；AAPL一股336.13美元BUY LIMIT DAY，先OPEN，查询后仅撤此单，远端确认canceled/filled_qty=0。没有第二笔远端订单。

首次最终观察因订单列表分页失败，诚实保存remote_observation_failed，未写成核对通过；finally仍经原执行服务完成取消。[官方订单列表定义](https://docs.alpaca.markets/us/reference/getallorders-1)的limit最大500；实际短页后使用after_order_id仍返回同一记录，原实现错误地要求短页后必须再有空页。修复仅使订单短页结束读取，满页仍继续且重复ID/游标仍拒；活动分页不变。旧测试改用limit=1制造真正满页，保留游标和重复拒绝断言，另增短页/内部重复反例，非作者复核通过。原始响应保存在pagination-observation.json；本轮不宣称已真实验证500条以上分页。修复后只恢复原订单，不再POST。

## 重启结果与独立复核

plan-04/observations/0002通过独立进程paper-recover取得：submitted=1、filled=0、pending=0、reconciled=true、queue_cancel_verified=true、queue_restart_verified=true；正常filled_and_reconciled/restart_verified保持false。远端CANCELED/0，开放订单与持仓为空；内部现金10000加固定reserve90000等于远端100000，活动与初态一致，events为空。0002 journal与0001逐项相等，仅intent/OPEN/CANCELED，没有重发或重复记账。非作者核对记录review-final-remote.md及review-preflight-adapter.md保存于证据目录。

uv8765和Conda8766两套展示均实际启动，HTTP输出完全一致；浏览器重新加载及点击刷新可见完整远端订单ID、已撤销、零成交和核对一致，单列“休市提交、撤单与重启核对”通过，正常成交闭环仍未完成。Conda验证服务随后停止，uv本地只读窗口保留；没有持续交易服务。原始运行资料仅本机artifacts保留，新检出须自行采集或提供保存的目录。

## 最终离线检查与发布边界

代码冻结后release检查：uv **439 passed / 36 warnings / 68.20秒**，Conda **439 passed / 36 warnings / 70.66秒**，两套quant-core check退出0，治理、Ruff格式/规则、严格mypy全部通过。共同requirements从uv.lock重新导出比对一致，无新增依赖。普通测试保持网络连接封锁，未接触真实账户。另以HEAD为基线输出敏感差异清单，治理无错误；根价格口径规则、锁/依赖、测试和风险改动按前轮与本轮记录完成独立复核。

此前名称含final的检查并非发布结果：uv为分页修复前436通过；Conda研究测试在运行期间检测到实际源码改变而明确失败（435通过、1失败），保留失败记录，未改断言。其后冻结源码重新顺序运行的release两套结果才是上面的最终439通过。第三方弃用警告未屏蔽。

凭据值扫描覆盖待提交源码/文档及509份本轮本地证据，均未发现凭据值；.env被忽略，原始账户证据不提交。Git发布仅包含获授权的前轮Alpaca接入、MA策略、本轮修复、展示、独立测试和文档。用户最新明确要求本轮订单操作验收后提交推送；不把Git发布等同策略成交或实盘部署。提交记录采用英文feat标签及中文说明，实际提交身份以Git历史为准。

两环境最终原离线demo、validate、replay均成功，回放五项true、10份业务JSON精确一致；新目录均带release后缀。沙箱PyArrow CPU探测诊断保留，不影响退出码或验证结果。
