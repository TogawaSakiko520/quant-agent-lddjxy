# 文档维护准则补强记录（2026-09-20）

本记录针对起始提交 `9d04aa52911ff621cdbed0073887a38d6313eb99`。工作区开始时干净；编辑前在新的 `artifacts/documentation-guidance-20260920/` 保存 158 个受版本管理的非凭据文件及哈希，排除凭据模板，不读取或复制私有凭据。后续检查使用该工作区快照，既有运行证据和历史审计保持原样。

## 问题、修改与边界

文档分工与统一表达原则已有基础，但通用维护步骤偏向代码任务，缺少编辑前后的判断方法；长期规则和当前状态还残留旧任务口吻。现有检查能验证部分结构条件，不能判断可读性、内容归属或确认独立复核已经发生。

此次按明确授权补强 QC-014/015 的维护说明：根规则说明长期规范、任务要求和历史事实如何归位，以及修改规则自身的依据与复核；文档原则提供按任务取舍的判断问题；维护手册区分文档、注释和业务路径；接手、实现、复核提示词引用共同入口并区分作者与复核者职责。判断问题不要求逐题填报、输出完整思考过程或机械评分，普通措辞改动不必新增审计文章。

PROJECT_STATE 区分业务验证基线 `85b9057`、文档提交 `f64f11b` 和协作合并 `9d04aa5`，清理旧任务“不提交”等临时表述。早先整理记录中的未提交结论仍按当时任务解释，不改写为后来事实。根规则的注释增减要求改为稳定措辞，中文 docstring、类型、独立预期测试、风控和权限约束保持。

没有新增 Meta 文件或规则版本体系，没有修改代码、Schema、配置、测试、检查器、CI 或依赖。需求映射仅在 QC-014/015 的文档列表加入本记录，原行为要求、实现和测试映射不变。README、操作命令及历史审计无需随本次规则说明重写，保留原文。

## 场景走读

以下是对修改后规则及提示的人工场景推演，不是自动测试通过率，也不保证任意 AI 都会正确执行。

| 场景 | 依据新提示应作的判断与结果 |
|---|---|
| 新增教学文章 | 先确认独立读者与用途；确有必要时保留专题深度，只在文档导航加入口，不将全文复制到 README 或规范。现有数学模型文章符合这一归属。 |
| 修改操作命令 | 先核对实际 CLI 和环境，再更新主要操作页及受影响的摘要/参考；文档任务不借机改变源码，必要前提不因精简而删除。 |
| 归档旧验收 | 当前页保留有效结论与证据入口，迁移维护链接并保留原日期、基线和失败；不把旧结论改写为新验收结果。 |
| 后续提交使任务表述过期 | 核对 Git 与验证证据，区分文档提交和业务验证基线；更新当前摘要，历史“未提交”仍保留当时含义，不延续旧任务授权。 |

## 实际验证与独立复核

使用已有 uv 与 Conda 项目环境，Python 均为 3.12.14，顺序运行以避免账户锁争用；没有安装或升级工具。uv 先执行 `uv sync --offline --locked --check`，Conda 先在专用环境执行 `python -m pip check`，均通过。完整命令、退出码和日志见新证据目录的 `validation-commands.json` 及各命令日志。

| 本次实际检查 | uv | Conda |
|---|---|---|
| `quant-core check --base 9d04aa52911ff621cdbed0073887a38d6313eb99` | 退出 0，439 项通过、36 条警告 | 退出 0，439 项通过、36 条警告 |
| 新目录 demo → validate → replay | 三步退出 0 | 三步退出 0 |
| 回放账户、订单、因子、信号、目标 | 五项均为 true | 五项均为 true |

两环境的 10 类业务 JSON 一致，结果见 `result-verification.json`。依赖弃用警告及沙箱下 PyArrow 的 CPU 信息读取提示保留原日志，没有过滤警告或改变断言。本次不重做新机安装、展示窗口或真实账户验收；这些入口未修改，不将以前的验收冒称本次执行。

核验 266 处本地文档链接、标题锚点及需求路径，无错误；其中 16 处历史 `artifacts/` 链接单独分类，不假定新检出包含生成证据。与起始快照比较，106 个非文档且非需求映射文件字节未变，所有旧审计未变，根规则“不可突破的约束”段原文保持；需求的实现/测试映射未变，`git diff --check` 通过。范围和链接结果见 `scope-and-links.json`。

非作者独立复核通过，记录为 `review.md`：独立比较基线、检查权限与注释保护，并推演上述四类场景；没有发现规则重复成第二套标准、强制填报或借文档路径弱化既有测试的问题。作者验证与独立复核分别留证，不以场景推演替代实际命令结果。

## 保留的限制与后续事项

一般 Markdown 链接/锚点尚未纳入常规治理检查，`requirements.txt` 尚未纳入敏感文件分类；手册明确由复核补足，检查器保持原样。PR 强制审查、分支保护和自动检查补充需要独立确定范围，本次未查询或修改远端设置。

提示词与规则能提高持续遵循的可靠性，不能强制 AI 自动读取，也不能保证内容永不退化；执行者仍需实际核对事实，复核者仍需独立走读。没有访问账户、执行订单、安装升级工具或提交推送。
