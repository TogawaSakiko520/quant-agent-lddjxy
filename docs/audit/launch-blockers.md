# 模拟盘与实盘上线阻塞

日期：2026-09-13；审计基线 `f01a1237bc02deae4957ceecd29639e16037759d`。本页是修复与验收清单，不是变更风控或发送订单的授权。证据、实际命令和状态定义见 [readiness](readiness.md)。本轮所有缺陷均保持原实现，未改测试、风险配置、契约或锁文件。

最高优先级是 B01 风控时点缺陷、B02 缺真实接口/数据权限链、B03 小账户碎股缺口。离线 check 的117项通过不会抵消这些阻塞。

## 模拟盘阻塞

| ID / 状态 / 阻止阶段 | 风险与证据位置 | 修复目标 | 独立验收与关闭依据 |
|---|---|---|---|
| **B01 / 部分实现 / 任一Paper写入前** | 已复现F01：`risk.assess_order`在`risk.py:419`用未过滤的旧仓行业版本；未来A0版本让超限单通过。`portfolio.py:289`存在同类静态路径，尚未单测该反例。 | 风控和规划对全部旧仓、未成买单及新单统一使用当时已知、当日有效、唯一明确的主表行业；缺失/重叠/质量异常阻新增。复用既有时点模型，不能只相信新适配器预筛。 | 原固定输入中TECH=26,000 >24,999.75，两次均必须拒绝；未来版本插入顺序变化不得改变决策，过期/缺失/冲突分别拒绝。保留全部原断言。标准demo目前传过滤后的主表，不宣称其已触发此缺陷。 |
| **B02 / 未实现 / 只读连接与Paper闭环** | `application.py:303–307`只装FakeBroker，`contracts.Broker:680–714`是协议；`adapters/`及依赖无Alpaca。账户身份、entitlement、feed、分页、错误响应尚未验证。 | 用官方alpaca-py封装个人Trading API，行情单独适配；先只读账户/持仓/开放订单/成交/属性/时钟/日历，显式feed及分页、限流、时点证明。账户探测须在后续明确只读授权下执行。 | 无凭据HTTP样例覆盖多页、429、拒绝、feed变化、缺符号/缺日、更新时间；每页取全后才封印。账户级身份/权限核实当前为**外部阻塞、未执行**；拒绝SIP不购买或偷偷切IEX。IEX量不得冒称全市场ADV。 |
| **B03 / 部分实现 / 300美元Paper** | F03：`portfolio.py:178`取整；`contracts.py:188,364,391,442,463,504,533`数量为整数，无fractionable/notional/步长；`configs/demo.toml:10–13`仍100,000美元/20持仓示例。 | 数量、持仓、事件、目标、股息权益用有单位的十进制定点规则，明确最小金额/残余和现金预留；不可碎股资产按明确理由拒绝。新增专用小账户配置，经人工确认持仓数/风险预算，不能简单提高单票上限使测试通过。 | 固定300×4.5%÷1000=0.0135股；另外独立300/预算30/价格1000=0.03股用例须有单独合理配置。测试不可碎股拒绝、数量精度、qty/notional互斥、现金与费用、最小金额及残余；保留整股兼容样例。后续经授权只读确认远端Paper净值/现金均为300美元、无旧持仓/开放订单，且账户ID与凭据绑定匹配；新建/重置后必须重新核对，本任务不执行账户修改。当前不允许把市价伪装为限价或自动降级，碎股limit+DAY支持冲突保持阻塞。 |
| **B04 / 部分实现 / 单次与持续Paper** | `_recover:89–198`已做独立订单/事件/账户对账，但协议假设可读完整本地历史；真实stream、补页、序号映射、入出金/结算/费用活动缺失。`ledger.py:75,124`只实现合成即时可用现金。 | 把真实生命周期映射接入已有唯一执行入口，超时查询、不重发；恢复前取完整订单/成交/账户活动，留存差异，禁止覆盖内部账本消差。真实购买力与现金预算取保守边界，禁止融资/做空。 | 扩展A–E/G/J/N：4/10股崩溃后只续余6；撤单期间成交与旧状态不回退；分页中断不放行；入出金不能算收益；未成卖款200不使现金300+旧买占101的可用199变399。恢复点后未知意图冻结，不能补造授权。真实集成尚未执行。 |
| **B05 / 部分实现 / 持续Paper；F02单次监控也需修** | F02：`monitoring.py:123–144`漏掉目标外持仓；`application.py:249–250`只运行最后一次调仓，`:418–422`把心跳/来源时间设成执行时间且传固定完成步骤。无常驻调度、外部心跳、通知、值守。 | 监控检查目标与实际证券并集；新增最小单主机调度、到期/漏跑状态、持久暂停和启动先恢复；把真实监测事实注入已有规则。保留同机账户锁，避免第二执行器。 | 空目标、A=1必须TARGET_DEVIATION；目标内/外偏差均列出且不补单。DST/假日/半日市通过；停进程/漏一调仓必须被独立观察者发现；重启旧信号按期限拒绝而非补发；真实告警送达与人确认须后续授权验证。 |
| **B06 / 部分实现 / 增加真实SDK之前及Paper写入前** | DemoConfig仅offline/DEMO有效；没有paper/live端点、凭据/账户隔离。`authorized=True`是布尔上下文，非人类审批。`pyproject.toml:27–31` live marker未默认排除，socket fixture在导入后才启动。 | 保持offline关闭交易；增加只读/paper能力隔离、严格账户/交易与流端点白名单、live默认禁用。测试收集之前禁网、无凭据、默认排除外部测试；固定SDK锁实际解析且独立审查，不自动升级生产依赖。 | 固定错误组合paper+live端点、错误account ID、凭据模式不匹配均在submit前拒绝；只读路径不能调用写接口；导入期HTTP尝试必须失败；当前离线CLI/Schema/117项原测试保持通过。实际权限不靠AI自签。 |
| **B07 / 部分实现 / 持续Paper恢复与固定版本切换** | `sqlite_store.py:112–139`是同机操作flock；`tools.backup_sqlite:76`单库复制通过，不是跨库原子或完整灾恢。现有N只验证通用两事件SQLite。没有发布服务/完整恢复点后的外部事实演练。 | 先采用经验证的单主机单执行者；停写并备份整个运行清单/两库/配置/版本/幂等，恢复到新路径后先查外部事实；固定源码/锁/配置版本，禁止回滚数据库抹掉已发单。 | 备份后独立Fake成交4/10、恢复旧内部库后只补记4、保留余6；备份后新出现的未知订单冻结；篡改/缺manifest拒恢复。若转为多写者/跨主机，依ARCHITECTURE先另行验收事务存储与租约，不能拿本机flock抵用。 |

F01/F02是当前纯业务入口的已复现缺陷；其余多数是原离线范围外的能力缺口。不存在“因为未接Alpaca所以原离线工程完全没有价值”的结论。现有意图、事件去重、撤单竞态、恢复/回放、日历和备份工具应复用。

## 实盘额外阻塞

下面各项在模拟盘关键阻塞全部关闭之外仍独立成立。300美元是用户资金上限，不是实盘授权；零付费API约束不代表零出入金/交易成本。

| ID / 状态 | 风险与位置 | 修复/确认目标 | 关闭证据 |
|---|---|---|---|
| **L01 / 外部阻塞** | `ASSUMPTIONS.md`账户主体、权限与资金条件未确认；本机时区不证明居住地或开户资格。 | 人类确认居住地/KYC、资金来源、券商支持、账户类型/权限与实际限制；不由AI开户或调用Broker API资金接口。 | 账户持有人提供可核实确认；敏感材料留券商/受控渠道，审计报告仅记结论和日期，不留身份证件或密钥。 |
| **L02 / 外部阻塞** | 实际300美元初态、到账、结算、费用及退出资金通道未核实。`DemoConfig.minimum_fee=1`与即时结算不是实盘规则。 | 明确资金到账、可用购买力、券商实际收费/汇兑/银行费用，以及退出持仓和取回资金路径；预算上限涵盖适用成本，不融资或做空。 | 经授权只读核对实际净值/现金/持仓/开放订单与账户ID；券商/银行最新费用和政策确认。官方费表仅候选参考，不断言用户必付某项费用。 |
| **L03 / 部分实现** | `research.py:84,195`时间分割已实现，但`application.py:832`声明未扣成本；样本完全合成，无真实收益有效性证据。 | 获授权的免费数据、可说明的历史成员/公司行动范围，固定同一策略的成本后账户轨迹、基准、换手/回撤、分期及参数稳定性；披露IEX覆盖/幸存者偏差。 | 固定实验预算/版本与样本外结果，独立手算成本/NAV用例，全试验留痕；Paper盈利或20天正常运行不得替代。无合格数据时保持外部阻塞，不买数据绕过约束。 |
| **L04 / 外部阻塞** | `.github/workflows/quality.yml`只是本地配置；远端CI/保护分支、审批人/环境与固定发布/回滚未验收。布尔authorized不能证明人类授权。 | 人类批准固定代码/锁/策略与账户白名单、风险预算、资金限额，管理员实际设置保护；AI不能批准自己或扩大权限。 | 固定版本与独立审查、管理员设置证据、远端CI结果、受控发布/回滚演练。未执行就是未验证，不用本地JSON冒充审批。 |
| **L05 / 部分实现** | 运维仍是本地一次性规则，告警无实际owner/ack；没有无人值守事故处置证据。 | 明确值守人、通知渠道/升级时限、独立外部心跳，能从券商原生界面暂停/处理持仓，保证停机不会被误解为已撤单/已清仓。 | 实际告警送达与确认、掉线/重启/备份恢复演练，无未解释严重对账差异；至少20交易日且4次正常调仓的Paper工程观察建议，加全部关键故障覆盖。 |
| **L06 / 外部阻塞** | FINRA规则存在券商过渡期；账户规则不可统一硬编码。 | 向具体券商核对账户适用制度、迁移状态和附加要求，不根据旧PDT门槛或“已统一取消”推断资格；维持不借款、不做空。 | 官方规则与账户级确认均注明日期。2026-06-04生效及2027-10-20过渡终止的公开依据见[ADR](../adr/free-paper-route.md)，本轮未核具体账户。 |

持续Paper不具备条件；有人监督的小额实盘与无人值守实盘均不可上线。单次Paper也须在对应阻塞关闭并另获交易授权后执行。修复完成与实际放行由独立证据决定，不使用百分比评分或以其他功能得分抵消资金安全缺口。

## 独立复核和变更边界

本轮根代理实际运行统一检查及独立探针；两名分工复核者独立检查输入、公式、源码路径和日志。ADR经独立审查后明确保留限价保护，市价仅作另需人类风险批准的备选。上述AI复核是审计证据，不是发布审批。本页只记录要求；未改任何业务文件，故不触发本次Schema/公式/配置或测试期望更新。
