# 文档整理与验证（2026-09-20）

本次仅整理文档、维护提示词和需求中的文档路径，不修改 Python 源码/docstring、测试、治理检查器、CI、配置、锁或已有运行证据。不读取凭据、不访问账户、不操作订单、不安装升级工具、不 commit / push。

## 起点与内容归属

起始 HEAD 为 `85b90576d021dca4721779df5e8b897d09c2a2df`，工作区干净。编辑前在新的 `artifacts/docs-reorganization-20260920/baseline/` 保存 155 个非凭据版本文件及文档，`baseline.json` 留存 Git 状态和每文件 SHA-256；核验基于这份工作区快照，不只对比 HEAD。所有本次验证产物使用此新根目录，历史交易证据不动。

README 负责项目用途和入口选择；快速开始负责首次安装、demo、报告/展示与简单验证；Paper 手册负责真实模拟账户任务的具体步骤；`docs/commands.md` 集中完整参数、产物及退出码。环境工具/PATH 排障和共同依赖流程集中于原维护手册，补充身份/总回报字段格式归入原契约页，固定旧版故障归入恢复手册。

PROJECT_STATE 改为唯一当前状态；原状态和原 CHANGELOG 全文移至[历史归档](project-history-20260920.md)，只转换相对链接。旧审计及路线 ADR 只添加历史范围提示，正文与结论保留。CHANGELOG 按原日期保存主要变化并链接细节；历史权限不作为新任务许可。

根规则移出“已授权离线建设”“第二轮仅注释”的过期任务状态，保留中文注释/类型、独立测试/复核、时点、账务、风险、共同锁及权限保护。提示词和文档原则改为要求状态摘要与证据分工，避免持续复制实验全过程。

## 事实修正与影响

关联 QC-001/008/012/013/014/015，行为规格仅将“本轮 Paper”改为稳定的验收表述，业务含义不变。需求映射只追加受影响文档路径；因子与策略专题原文不强行改写，Schema、业务配置和原实现/测试映射不变。

- 备份手册不再笼统声明所有契约为 1.0.0，按实际四种模型默认 1.1.0 及受限旧版兼容说明。
- 首次 Paper 明确账户 ID 必须先确定；模板空值不能直接运行，CLI 没有自动发现账户 ID 的命令。
- MA 身份资料须在采集完成前已可用，后来取得应新目录重采，不回填时间；原双因子继续要求合格行业/总回报。
- 区分普通恢复无 300 秒观察窗口、休市测试恢复仍可能最多 60 秒撤单核对；撤单请求不等于完成。
- 示例统一为新检出可自行创建的路径，展示传完整采集/计划根目录；已有结果不需重跑，离线演示不能冒充 Paper 证据。
- 当前状态区分代码、离线、真实只读、策略计划、提交、撤单、零成交核对与尚未验收的实际成交。此前真实事实来自已有审计，不重新查询账户。

阅读发现的源码说明范围问题单独留记：`cli.py` 的模块/解析器 docstring 仍以离线六命令概述，实际已有四个显式 Paper 命令；本次按边界未修改源码说明。未由此推断业务故障或改变行为。

## 验证与复核

使用本机已有的 uv 0.12.5 / `.venv` 和项目专用 Conda 环境，两者 Python 均为 3.12.14。Conda 程序位于 `artifacts/conda-compatibility-20260919/miniforge/bin/conda`，环境位于 `artifacts/alpaca-paper-20260919/conda-env`；这些是本次机器证据，不是首次使用前提。完整命令、耗时、退出码见本次证据目录的 `validation-commands.json`。

| 验证 | uv | Conda |
|---|---|---|
| 现有环境 | `uv sync --offline --locked --check` 通过 | 环境内 `python -m pip check` 通过 |
| `quant-core check --base 85b90576d021dca4721779df5e8b897d09c2a2df` | 退出 0，439 passed / 36 warnings | 退出 0，439 passed / 36 warnings |
| `demo` → `validate` → `replay` | 三步退出 0 | 三步退出 0 |
| 回放比对 | account / orders / factors / signals / target 全为 true | 同左 |
| 展示打开与刷新 | 8767 端口通过 | 8768 端口通过 |

两环境顺序运行，避免相同账户锁争用。uv 运行设置 `UV_NO_SYNC=1`、`UV_CACHE_DIR=/private/tmp/quant-qc014-uv-cache`、`UV_PYTHON_DOWNLOADS=never`，先只读核验同步状态；Conda 使用已有环境的 `conda run --no-capture-output -p`。检查包含治理、格式、类型与全部测试，没有改断言或过滤警告。36 条警告来自现有依赖弃用提示；部分离线命令另有 PyArrow 读取受沙箱限制的 CPU 信息提示，均保留在原日志中，命令实际退出 0。

`uv-demo`、`uv-replay`、`conda-demo`、`conda-replay` 均为新目录。两个 demo 的账户、订单、因子、信号、目标、事件、分录、风险、市场状态和核对共 10 类 JSON 完全一致。浏览器实际打开两条展示入口并点击刷新，均明确显示离线 FakeBroker、20 笔完全成交和核对一致；这里的成交只属于合成演示。刷新前后保存文件哈希一致，两环境返回的展示数据一致。验证后停止本次两个临时服务并关闭临时标签，不操作用户原有的 8765 窗口。结果见 `result-verification.json`。

文档核验覆盖相对链接、标题锚点和全部需求路径，并将不随 Git 分发的历史 `artifacts/` 证据链接单独列出，不当作新检出已有文件。21 条 CLI 示例用实际解析器检查参数，27 个 shell 代码块通过语法检查；解析检查禁止 socket 连接，没有执行 Paper 命令。锁文件另行导出到本次证据目录，与 `requirements.txt` 的非注释内容完全一致，原锁和清单未改。记录分别为 `docs-verification.json`、`command-verification.json`、`dependency-verification.json`。

按新读者路径走读 README → 环境选择 → demo → 报告/展示 → validate/replay，两条路线均能找到前提、成功标志、结果路径和失败入口；Conda 路线无需 uv。Paper 主线仅对照当前实现和参数走读，确认两策略、休市测试与保存结果四类入口清楚，账户身份、资料时间、具体授权和未知状态恢复前提完整。

非作者独立复核检查实际代码、参数、历史迁移、权限与规则，初审指出账户 ID、身份资料时序和恢复等待三项说明问题，修正后复核通过，记录为 `review-initial.md` 和 `review-final.md`。最终范围核验与起始快照比较，107 个非文档且非需求映射文件字节未变，需求的 ID / 实现 / 测试映射未变；归档逆转相对链接后与原状态及 CHANGELOG 全文精确相同，旧审计撤去新增提示后也与基线相同。最终差异只含文档、提示词与需求文档路径，`git diff --check` 通过；最终范围与治理结果见 `final-scope-verification.json`。

安装/创建环境命令仅按当前配置、工具帮助及既有安装证据核对，本次未重装环境、未验证空缓存新机安装、其他操作系统或远端 CI。没有访问真实账户，未执行 Paper 下单/撤单/恢复，也未重新核实外部资料现时性。真实成交等业务待验收项仍按 PROJECT_STATE 保留。
